# Skill metadata for trace-requirement.
# Explicit-only: this is a deliberate pre-flight gate ($trace), not something
# Codex should trigger on its own guess that a task matches.
display_name: "Trace requirement ($trace)"
allow_implicit_invocation: false
